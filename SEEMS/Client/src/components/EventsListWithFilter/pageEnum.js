const pageEnum = {
    AllEvents: '/events',
    MyEvents: '/events/me',
    MyRegistrations: '/events/my-registrations',
    AdminAllEvents: '/admin/events',
    AdminMyEvents: '/admin/events/me',
}

export default pageEnum
